function solution(A,B,C) {
    return Math.floor((B-A)/C)+1
  };

function solution2(A,B) {
    var result = 0;
    for (i=A;i<B;i++) 
      for (j=i+1;j<=B;j++){
      result = Math.max(result, i^j)
    }
    return result
  };
